<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PrintCloud</title>
    <link rel="stylesheet" href="main.css">
    <link rel="stylesheet" href="header.css">
    <link rel="icon" type="image/x-icon" href="picture/logo.png">
    <% if (userFound) { %>
        <p id="username" style="display:none"><%= userFound.user %></p>
        <p id="pas" style="display:none"><%= userFound.password %></p>
        <p id="email" style="display:none"><%= userFound.email %></p>
    <% } %>
</head>
<body>
    <script src="js/index.js"></script>
    <script src="js/languare.js"></script>
    <header>
        <div class="headerb2">
            <select class="headerb" id="lang" onchange="serLanguare()">
                <option selected>Deutsch</option>
                <option>Englisch</option>
            </select>
            <form class="headerb" action="modells">
                <input name="search"  placeholder="Search..." type="search">
            </form>
            <% if (userFound) { %>
                <p id="username" style="display:none"><%= userFound.username %></p>
                <p id="email" style="display:none"><%= userFound.email %></p>
                <p id="pas" style="display:none"><%= userFound.password %></p>
                <p>Hallo, <%= userFound.username %></p>
                <button onclick="logout()">Logout</button>
                <a href="cratemodell">
                    <button class="headerb">Erstellen</button>
                </a>
            <% } else { %>
                <a class="headerb" href="login">
                    <img class="aimg" src="/picture/accounnt.png" alt="">
                </a>
            <% } %>
            
            
        </div>  
        <div class="headera2">
            <a class="headera" id="home" href="/">Home</a>
            <a class="headera" id="comunity" href="comunity">Comunity</a>
            <a class="headera" id="modells" href="modells">Modells</a>
        </div>    
    </header>
    <main>
        <section id="info" style="background-image: url('picture/banner.png');">
            <h2 id="Hello">Welcomme to PrintCloud</h2>
            <br>
            <h3 id="Hello2">The best side for 3d Printing 3d Modells</h3>
        </section>
    
        <section id="inspiration">
            <script src="https://js.puter.com/v2/"></script>
            <script>
                document.addEventListener("DOMContentLoaded", function() {
  // Pop-up entfernen, falls vorhanden
  const popup = document.querySelector('putter-dialog');
  if (popup) {
    popup.remove(); // Entfernt das Pop-up
  }

  // Den Link finden, der das Pop-up auslöst und den Button klicken
  const continueButton = document.getElementById("launch-auth-popup");

  if (continueButton) {
    // Warte 1 Sekunde und klicke automatisch auf den Button
    setTimeout(function() {
      continueButton.click(); // Simuliere einen Klick auf den "Continue"-Button
    }, 1000); // Warte 1 Sekunde, um sicherzustellen, dass der Button verfügbar ist
  }
});

       




            
            </script>
            <style>
              .button-container {
                display: flex;
                gap: 10px;
                flex-wrap: wrap;
                margin-top: 20px;
              }
          
              .button-container form {
                flex: 1;
              }
          
              .button-container button {
                width: 100%;
                padding: 10px;
                font-size: 16px;
                cursor: pointer;
              }
            </style>
          
            <div class="button-container">
              <!-- 5 Buttons -->
              <form action="modells">
                <input type="hidden" name="search" class="hiddenInput">
                <button type="submit">
                  <span class="ide">Lade Inspiration…</span>
                </button>
              </form>
          
              <form action="modells">
                <input type="hidden" name="search" class="hiddenInput">
                <button type="submit">
                  <span class="ide">Lade Inspiration…</span>
                </button>
              </form>
          
              <form action="modells">
                <input type="hidden" name="search" class="hiddenInput">
                <button type="submit">
                  <span class="ide">Lade Inspiration…</span>
                </button>
              </form>
          
              <form action="modells">
                <input type="hidden" name="search" class="hiddenInput">
                <button type="submit">
                  <span class="ide">Lade Inspiration…</span>
                </button>
              </form>
          
              <form action="modells">
                <input type="hidden" name="search" class="hiddenInput">
                <button type="submit">
                  <span class="ide">Lade Inspiration…</span>
                </button>
              </form>
            </div>
          
            <script>
              // Alle Inspirationen laden, wenn die Seite lädt
              document.querySelectorAll(".button-container form").forEach(form => {
                let ideSpan = form.querySelector(".ide");
                let hiddenInput = form.querySelector(".hiddenInput");
          
                puter.ai.chat("Nenne mir ein einziges kreatives, veganes, nicht rassistisches und sinnvolles deutsches Stichwort für ein 3D-Druck-Projekt. Es darf nichts mit Vasen, Skulpturen, Schmuck, Lampen oder Schlüsseln zu tun haben. Gib nur ein einzelnes Wort ohne Satz und ohne Anführungszeichen zurück.")
                  .then(response => {
                    ideSpan.textContent = response;
                    hiddenInput.value = response;
                  });
              });
            </script>
          </section>
        <section id="bestmodell">
            <h3 id="efm">Featured Models</h3>
            <div id="model-list-2">
                <% models.slice(0, 4).forEach((model) => { %>
                    <div class="boarderb">
                        <a href="modelldet?id=<%= model.id %>" style="display: block;
                            text-decoration: none; 
                            color: inherit; ">
                            <strong>By: <%= model.userb %></strong>
                            <% if (model.pictureFiles && model.pictureFiles.length > 0) { %>
                                <img src="/models/file/<%= model.pictureFiles[0] %>" width="200" height="200" style="object-fit: contain; background-color: white;"><br>
                              <% } else { %>
                                <img src="/picture/placeholder.png" width="200" height="200" style="object-fit: contain; background-color: white;"><br>
                              <% } %>
                         <br>
                            <strong><%= model.name %></strong>
                            <form action="/like" method="get">
                                <input type="hidden" name="id" value="<%= model.id %>">
                                <input type="radio" name="like" value="on" id="like" onchange="this.form.submit()">
                                <label for="like">👍</label>
                                <p><%= model.like %></p>
                            </form><p>10⬇️</p>
                        </a>
                    </div>
                        
                <% }); %>
              </div>
            
    
        </section>
        <br>
        <section>
            <a href="modells">
                <button class="button" id="Explorebutton">Explore Modells</button>
            </a>
            
        </section>
        <section>
            <h3>Best Creators</h3>
            <div>
                
            </div>
        </section>
        <section style="background-color: white;">
            <h3 id="newmodells">New Modells</h3>
            <div id="model-list-2" onclick="modelldet()">
                <% models.slice().reverse().slice(0, 4).forEach(model => { %>
                    <div class="boarderb">
                        <a href="modelldet?id=<%= model.id %>" style="display: block;
                            text-decoration: none; 
                            color: inherit; ">
                            <strong>By: <%= model.userb %></strong>
                            <% if (model.pictureFiles && model.pictureFiles.length > 0) { %>
                                <img src="/models/file/<%= model.pictureFiles[0] %>" width="200" height="200" style="object-fit: contain; background-color: white;"><br>
                              <% } else { %>
                                <img src="/picture/placeholder.png" width="200" height="200" style="object-fit: contain; background-color: white;"><br>
                              <% } %>
                            <br>
                            <strong><%= model.name %></strong>
                            
                            <form action="/like" method="get">
                                <input type="hidden" name="id" value="<%= model.id %>">
                                <input type="radio" name="like" value="on" id="like" onchange="this.form.submit()">
                                <label for="like">👍</label>
                                <p><%= model.like %></p>
                            </form><p>10⬇️</p>
                        </a>
                    </div>
                <% }); %>
                <!-- Neuste Modelle -->
              </div>
        </section>
    </main>
    <footer>
        <p>2025 © Finn3d</p>
        <a href="">Nutzungsbendingungen </a><a href="">Datenschutzerklärung</a>
    </footer>
</body>
</html>